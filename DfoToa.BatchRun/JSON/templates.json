{
  "responsible": {
    "externalId": "string([any string]|{employee.id}|[any string]|{employee.email}|[any string]|{caseManager.id}|[any string]|{caseManager.email}|[any string])",
    "email": "string([any string]|{employee.id}|[any string]|{employee.email}|[any string]|{caseManager.id}|[any string]|{caseManager.email}|[any string])"
  },
  "titles": {
    "file": "string([any string]|{contract.sequenceNumber}|[any string]|{contract.contractNumber}|[any string]|{employeeContract.startDate}|[any string]|{employeeContract.endDate}[any string])"
  }
}