# ASP.NET
# Build and test ASP.NET projects.
# Add steps that publish symbols, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/aspnet/build-aspnet-4

trigger: 
- feature/*
- task/*
- bug/*
- issue/*
- dev/*

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

stages:
- stage: build

  jobs:
  - job: buildAndTest
    displayName: 'Build and test project'
    steps:
    - task: NuGetToolInstaller@1

    - task: NuGetCommand@2
      inputs:
        command: 'restore'
        restoreSolution: '$(solution)'
        feedsToUse: 'select'
        vstsFeed: 'fdad029d-ef21-4332-8721-280783508a77'

    - task: CmdLine@2
      inputs:
        script: copy $(Build.SourcesDirectory)\JSON\_general.example.json $(Build.SourcesDirectory)\JSON\_general.json
      
    - task: VSBuild@1
      inputs:
        solution: '$(solution)'
        platform: '$(buildPlatform)'
        configuration: '$(buildConfiguration)'

    - task: VSTest@2
      inputs:
        testSelector: 'testAssemblies'
        testAssemblyVer2: |
          **\$(BuildConfiguration)\**\*dfo-toa-manual.test.dll
          !**\$(BuildConfiguration)\**\*Models*.dll
          !**\*TestAdapter.dll
          !**\obj\**
          !**\$(BuildConfiguration)\**\*IntegrationTests*.dll
        codeCoverageEnabled: false
        testRunTitle: '$(Build.DefinitionName) | $(Build.Reason) |$(Build.SourceVersion)'
        platform: '$(buildPlatform)'
        configuration: '$(buildConfiguration)'

    - task: CopyFiles@2
      inputs:
        contents: |
          bin\**\?(*.exe|*.dll|*.exe.config)
        targetFolder: $(Build.ArtifactStagingDirectory)
        flattenFolders: true
      displayName: 'Copy artifacts to staging directory'
    - task: CopyFiles@2
      inputs:
        contents: |
          JSON\_general.json
        targetFolder: $(Build.ArtifactStagingDirectory)
        flattenFolders: false
      displayName: 'Copy _general.json to staging directory'

    - task: PublishBuildArtifacts@1
      inputs:
        pathToPublish: $(Build.ArtifactStagingDirectory)
        ArtifactName: artifacts.feature.$(Build.BuildNumber)
      displayName: 'Publish artifacts'

